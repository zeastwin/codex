<Window x:Class="StressTest.DocWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        mc:Ignorable="d"
        Title="StressTest 使用说明"
        Width="860"
        Height="900"
        MinWidth="720"
        MinHeight="580"
        FontFamily="HarmonyOS Sans SC, Microsoft YaHei UI"
        WindowStartupLocation="CenterOwner"
        Background="#FFFFFF"
        WindowStyle="None"
        ResizeMode="NoResize">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CornerRadius="12"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="0"
                            CaptionHeight="0" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <SolidColorBrush x:Key="CardBg" Color="#FFFFFF" />
        <SolidColorBrush x:Key="MutedText" Color="#64748B" />
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBg}" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="14" />
        </Style>
    </Window.Resources>

    <Grid Margin="0">
        <Border Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="关闭"
                            Padding="12,4"
                            Height="30"
                            Background="#E2E8F0"
                            Foreground="#0F172A"
                            BorderBrush="#CBD5E1"
                            BorderThickness="1"
                            Click="CloseButton_Click" />
                </StackPanel>
                <WebBrowser x:Name="ContentBrowser"
                            Grid.Row="1"
                            Margin="0,10,0,0" />
            </Grid>
        </Border>
    </Grid>
</Window>
