<Window x:Class="EW_Assistant.Views.Inventory.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="Microsoft YaHei"
        FontSize="13"
        MinWidth="360">
    <Border Margin="0" Padding="16" Background="White" BorderBrush="#D5D7DC" BorderThickness="2" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="PromptTextBlock"
                       Text="请输入"
                       FontSize="14"
                       Foreground="#111827"
                       Margin="0,0,0,10"
                       TextWrapping="Wrap"/>

            <TextBox x:Name="InputTextBox"
                     Grid.Row="1"
                     MinWidth="300"
                     Padding="10,8"
                     Background="#F2F4F7"
                     BorderBrush="#D1D5DB"
                     BorderThickness="1"
                     TextWrapping="NoWrap"/>

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,14,0,0">
                <Button Content="取消"
                        Width="80"
                        Height="30"
                        Margin="0,0,8,0"
                        Click="Cancel_Click"
                        Background="#F3F4F6"
                        BorderBrush="#D1D5DB"/>
                <Button Content="确定"
                        Width="90"
                        Height="30"
                        Click="Ok_Click"
                        Background="#3B82F6"
                        Foreground="White"
                        BorderBrush="#3B82F6"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
